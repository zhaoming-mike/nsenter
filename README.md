Mac platform docker base dir enter tools
========================================

# BUILD

``` shell
docker build -t zhaoming23/nsenter:latest .
```

# USAGE

``` shell
docker run -it --rm --privileged --pid=host zhaoming23/nsenter
```
